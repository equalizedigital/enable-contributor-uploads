{
  "name": "enable-contributor-uploads",
  "version": "1.0.0",
  "description": "Allows users with the contributor role to upload images.",
  "author": "Equalize Digital",
  "license": "GPL-2.0+",
  "main": "index.js",
  "scripts": {
    "dist": "composer install --no-dev && composer dump-autoload --no-dev -o && ./scripts/dist.sh && composer install",
    "lint": "./vendor/bin/phpcs",
    "lint:fix": "./vendor/bin/phpcbf",
    "prepare": "husky"
  },
  "files": [
    "LICENSE.txt",
    "readme.txt",
    "enable-contributor-uploads.php",
    "inc"
  ],
  "devDependencies": {
    "@wordpress/scripts": "^26.5.0",
    "husky": "^9.0.6",
    "patch-package": "^8.0.0"
  }
}
